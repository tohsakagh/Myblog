<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gh.myblog_backend.mapper.BlogMapper">

    <select id="countAllView" resultType="java.lang.Integer">
        select sum(views) from t_blog
    </select>
    <select id="countAppreciate" resultType="java.lang.Integer">
        select sum(appreciation) from t_blog
    </select>
</mapper>
